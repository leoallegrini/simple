package tup.simple.repositories;

import org.springframework.data.repository.CrudRepository;

import tup.simple.models.directores;

public interface directoresRepository extends CrudRepository<directores, Long> {
    
}
